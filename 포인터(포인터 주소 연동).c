#include <stdio.h>
/**
포인터 주소값을 동기화 시키면, 주소가 연동된 변수의 값도 포인터 변수를 변화시켰을 때 같이 바뀐다. 
**/
int main(void) {
	int x=10;
	int *p;//포인터 변수 
	p=&x; //포인터 변수의 주소값 동기화
	printf("x값=%d",x);
	*p=20;//포인터 변수의 값을 초기화시켜주면, 
	printf("x값=%d",x); //연동된 x의 값도 바뀐다. 
} 
